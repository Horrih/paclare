[project]
name = "paclare"
version = "0.1.0"
description = "Maintain your installed packages from a source file"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
paclare = "paclare.main:main"

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"
